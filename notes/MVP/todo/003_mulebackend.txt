basics
X- webpack backend to node-readable module for mule backend to consume
 - setup backend 'prod' build (uglifed and saved in dist/)
 - setup frontend 'prod' build (uglifed and saved in dist/)

then
 - see gen map & placement actions in a "real" mule environment
 - update jenkins
 - update mule-frontend to link to battleship
