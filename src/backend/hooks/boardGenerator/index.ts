
import { MuleStateSdk, BoardGeneratorHook, VariableMap } from 'mule-sdk-js';

const boardGenerator: BoardGeneratorHook = (customBoardSettings: VariableMap, battleshipRules: VariableMap) => {
  throw 'nyi';
};

export default boardGenerator;
